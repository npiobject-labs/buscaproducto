# Sesión 2026-09-13 21:26 — Desarrollo de principio a fin (F1–F8)

1. **Qué cambió:** el proyecto entero. `app/` pasa de Rust a **Python 3.12 + FastAPI** (D-01) con SQLite en volumen de Fly, API `/api/v1` protegida por `X-Clave`, búsquedas asíncronas con progreso por fuente, cliente HTTP respetuoso (robots, rate limit, caché, circuit breaker, detección anti-bot), adaptadores `enlace`/`universal`(IA)/`ebay`/`google-shopping`(Serper)/`chollometro`, capa de IA con cinco agentes (Intérprete, Extractor, juez de duplicados, Investigador con citas, Comparador, Reparador) sobre el gateway `apisor.oracle402.com` (D-13), histórico con veredicto de precio, alertas por Telegram, cosechador nocturno con Playwright en Actions y 33 tests. `docs/index.html` es ya la **app real** (PWA con modo demo integrado, build `BU-B1-20260913-003`), verificada con Playwright: 27 comprobaciones en móvil y escritorio sin errores de consola. Workflows nuevos: `ci.yml`, `fuentes.yml`, `vigilar.yml`, `cosechar.yml`; `deploy.yml` ampliado con volumen, secretos, humo de la API y volcado de logs si falla.
2. **SHA:** `ff20353` (rama `claude/sleepy-volta-kud4np`, PR #1 en borrador). Antes: `d89de23` (arreglo del arranque en Fly), `24b1a1e` (documentación del estado real).
3. **URL para probar:** tras fusionar en `main`, https://npiobject-labs.github.io/buscaproducto/ (funciona en modo demo sin backend). El backend queda en `https://buscaproducto-npiobject-labs.fly.dev/salud`; `/api/v1` responde 503 hasta que exista el secreto `BP_CLAVE`.
4. **Drive:** copia de `docs/planificacion/` en `Mi unidad/buscaproducto` de `fsantagonza@gmail.com` (id `121ANQkl…`), con el resumen en `sesiones/`. El id de `CLAUDE.md` apuntaba a otra cuenta y se corrigió en esta sesión.
5. **Qué falta:** fusionar la PR; crear los secretos (`BP_CLAVE` primero, luego `BP_IA_TOKEN`, `SERPER_API_KEY`, `EBAY_*`, `FIRECRAWL_API_KEY`, `TELEGRAM_*`) para poder medir en vivo los criterios C1, C2, C5 y C6; decidir sobre Keepa/Apify (D-15). Detalle en la sección «Estado tras la sesión» de `HOJA_DE_RUTA.md`.
